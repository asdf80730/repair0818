# v1.1.14 變更需求清單

> 承接 v1.1.13（已封存至 `docs/archive/v1.1.13-變更需求報告.md`）。本檔只列**尚未施工**的待辦項目，施工完成後移入 SPEC 版本歷程並封存。
> 版本：v1.1.14 ｜ 日期：2026-08-22

---

## 待辦項目（依優先序，含預計改動方式）

### A. 需業主決策（先確認再做）

| # | 項目 | 預計改動方式 | 來源 |
|---|---|---|---|
| A1 | 詳情 API 合併查詢 | `src/routes/tickets.ts` 詳情 handler：ticket+photos 一次、updates+updatePhotos 用 `json_group_array` 一次，4 次 roundtrip→2 次，目標 <400ms。需回歸測試 | 第四/八輪 |
| A2 | CSV 加發包金額/時間 | `src/routes/exports.ts` 第 12、13 欄加「發包金額、發包時間」，SELECT 加 `t.amount, t.amount_at`。契約變更，需出 diff 確認 | 第四/八輪 |
| A3 | 統計完成率語意 | 二選一：① UI 加註「完成率以本月結案/本月新增計算」；② 改定義「本月結案/(期初未結案+本月新增)」。需業主選 | 第八輪 |
| A4 | 統計頁月份切換 | `public/app.js` P5 加月份下拉，`GET /api/stats/summary?month=` 已支援，前端帶參數重抓 | 第四/八輪 |
| A5 | 逾期案件 Tab | `public/app.js` P1 tabs 加「逾期未更新」，篩選 `now-last_activity_at>7天` | 第四/八輪 |
| A6 | session 滑動續期 | `src/lib/auth.ts` requireAuth 判斷 `exp-now<900` 時 Set-Cookie 換發新 JWT（仍查 D1 active） | 第四/八輪 |
| A7 | CI 部署版本比對 | `/api/hello` 回傳 `{commit: env.CF_PAGES_COMMIT_SHA}`，CI 輪詢比對 `github.sha` 確認新版上線再跑 E2E | 第十一輪 |
| A8 | 照片選擇器 E2E | `e2e/app.spec.js` 補「選照片/超5張阻擋/✕刪除」測試（mock 需有照片上傳 handler） | 第十一輪 |
| A9 | void/reopen UI E2E | `e2e/app.spec.js` 補 ⋮ 選單作廢（二次確認）/reopen modal 互動測試 | 第十一輪 |
| A10 | committee CSV 403 測試 | `tests/boundary.test.ts` 補「committee POST /exports/sign → 403」（用 loginAs helper） | 第十一輪 |

### B. 需維運作業（非改碼）

| # | 項目 | 說明 |
|---|---|---|
| B1 | RateLimit | Cloudflare Dashboard 對 share/auth/session/CSV 公開端點設每 IP 限制（SPEC §10 已寫明）。建議：session 20/min、share 60/min、CSV 10/min |

### C. 低優先（可做可不做）

| # | 項目 | 預計改動方式 | 來源 |
|---|---|---|---|
| C1 | 分享頁顯示金額？ | 產品決策，需業主確認金額是否算內部資訊 | 第四輪 |
| C2 | 時間軸歷史金額顯示 | `public/app.js` 詳情時間軸列出每筆 `ticket_updates.amount`（資料已有） | 第四輪 |
| C3 | vendors 複合索引 | migration 0009：`CREATE INDEX idx_vendors_active_sort ON vendors(active, sort_order, id)` | 第九輪 |
| C4 | 時間軸不可變 Trigger | migration 0009：`CREATE TRIGGER` 擋 UPDATE/DELETE ticket_updates | 第九輪 |
| C5 | 逾期 Pill 膠囊 | `public/style.css` `.stale` 改淡橘底深橘字小膠囊（`background:#fef3c7;color:#92400e;padding:2px 6px;border-radius:4px`） | 第九輪 |
| C6 | assoc IN 子查詢索引 | 確認 `idx_options_type(type, active, sort_order)` 是否覆蓋 `option_categories` 全量覆寫刪除的子查詢；不足則 migration 補 `(type,id)` 索引 | 第十輪 |
| C7 | PRAGMA foreign_keys | `tests/apply-migrations.ts` 套 migration 前加 `await env.DB.exec('PRAGMA foreign_keys = ON')` | 第十輪 |
| C8 | select 長文字 ellipsis | `public/style.css` `.select` 加 `text-overflow: ellipsis; overflow: hidden` | 第十一輪 |
| C9 | assoc-list 高度放寬 | `public/style.css` `.assoc-list` max-height 200px→40~50vh + `overscroll-behavior: contain` | 第十一輪 |
| C10 | P0 pending 自動輪詢 | `public/app.js` pending 頁加 `setInterval(checkAuthMe, 5000)`，開通後自動跳主頁 | 第十一輪 |
| C11 | loading 防抖 | `public/app.js` 切 Tab 時 200ms 延遲才顯示 spinner，<200ms 直接渲染 | 第十一輪 |

### D. 已施工（第九輪審查後）

| # | 項目 | commit |
|---|---|---|
| D1 | share 頁底部 70px 空白 | 已修：share.html.ts `<body style="padding-bottom:16px">`（2f9b747） |

---

## 審查核對紀錄（第十輪、第十一輪）

### 第十輪：LINE 生命週期/Schema/前端狀態（11 條）
- **已做/誤判**：label trim、void 編輯按鈕、cleanUrlParams hash、last_activity_at 同一 now、system status NULL、ID 補零、submit finally
- **真實新發現**：assoc 索引（C6）、PRAGMA FK（C7）
- **不需做**：display_name 不同步（設計選擇，尊重後台改名）

### 第十一輪：測試架構 + UX/UI（10 條）
- **誤判**：thumb-del 22px（已改 32px）、stale 顏色（已改橘色）
- **真實新發現**：CI 幽靈檢驗（A7）、照片 E2E（A8）、void/reopen E2E（A9）、committee CSV 403（A10）、select ellipsis（C8）、pending 輪詢（C10）、assoc 高度（C9）、loading 防抖（C11）

---

## 施工避坑指南（第十二輪補充）

> 針對 A 類核心項目的實作陷阱，施工時務必遵守。

### A1 詳情合併查詢 — json_group_array 空值陷阱
- `LEFT JOIN photos` + `json_group_array` 在**無照片**時會產出 `[{"id":null}]` 而非 `[]`。
- 用 `CASE WHEN COUNT(p.id)=0 THEN json('[]') ELSE json_group_array(...) END` 防坑。

### A4 統計頁月份切換 — 雙 API 同步
- P5 有兩區塊：`/api/stats/summary?month=` 與 `/api/stats/amount-by-category?month=`。
- 切月份時用 `Promise.all([summary, amountByCategory])` 同步刷新，避免上下月份不一致。
- **現況**：目前分開抓（summary `.then`、amount `await`），需改 Promise.all。

### A5 逾期 Tab — 排除已結案/作廢
- 篩選 `>7天` 務必排除 `done`/`void`。
- **現況**：卡片 stale 已排除（`status==='open'||'in_progress'`），新增 Tab 沿用此條件。

### A6 session 滑動續期 — Cookie 屬性一致
- 換發 JWT 的 `Set-Cookie` 屬性須與登入一致：`Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=3600`。
- 注意 Hono middleware 順序，避免換發的 Set-Cookie 被後續 handler 覆寫。

### A7 CI 版本比對 — CF_PAGES_COMMIT_SHA
- Cloudflare Pages 建置自動注入 `CF_PAGES_COMMIT_SHA`。
- `/api/hello` 回傳 `{ ok, commit: env.CF_PAGES_COMMIT_SHA || 'dev' }`，CI 比對 `res.commit === github.sha`。
- **現況**：hello 未回傳 commit，需改。

---

## 施工順序（相依性最佳化）

1. **第一階段：測試基建** — A7（CI commit 比對）、A10（committee 403 測試）、C7（PRAGMA FK）
2. **第二階段：後端/DB** — A1（詳情合併）、A2（CSV 加欄）、A6（滑動續期）、C3（vendors 索引）、C4（時間軸 Trigger）
3. **第三階段：前端/UX** — A3（完成率）、A4（月份切換）、A5（逾期 Tab）、C5/C8/C9/C10/C11
4. **第四階段：E2E 收尾** — A8（照片 E2E）、A9（void/reopen E2E），CI 綠燈後移入 SPEC 版本歷程

---
> 施工完成一項，移入 SPEC 版本歷程 v1.1.14 並於本檔標記。
