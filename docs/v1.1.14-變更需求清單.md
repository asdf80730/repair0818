# v1.1.14 變更需求清單

> 承接 v1.1.13（已封存至 `docs/archive/v1.1.13-變更需求報告.md`）。本檔只列**尚未施工**的待辦項目，施工完成後移入 SPEC 版本歷程並封存。
> 版本：v1.1.14 ｜ 日期：2026-08-22

---

## 待辦項目（依優先序）

### A. 需業主決策（先確認再做）

| # | 項目 | 說明 | 來源 |
|---|---|---|---|
| A1 | 詳情 API 合併查詢 | 4 次 D1 roundtrip（~1s）→ 單一 SQL 聚合（json_group_array），目標 <400ms。效能重構、風險較高 | 第四/八輪 |
| A2 | CSV 加發包金額/時間 | 目前 11 欄無金額，管委會月會對帳需要。契約變更，需出 diff 確認 | 第四/八輪 |
| A3 | 統計完成率語意 | 「本月完成/本月新增」消化積案時可能 >100%。改定義或 UI 加註 | 第八輪 |
| A4 | 統計頁月份切換 | 後端已支援 `month=`，前端補下拉 | 第四/八輪 |
| A5 | 逾期案件 Tab | 前端已有 stale 計算，P1 加「逾期未更新」快捷 Tab | 第四/八輪 |
| A6 | session 滑動續期 | 剩 <15min 換發 Cookie，減少外部瀏覽器被踢 | 第四/八輪 |

### B. 需維運作業（非改碼）

| # | 項目 | 說明 |
|---|---|---|
| B1 | RateLimit | Cloudflare Dashboard 對 share/auth/session/CSV 公開端點設每 IP 限制（SPEC §10 已寫明）。建議：session 20/min、share 60/min、CSV 10/min |

### C. 低優先（可做可不做）

| # | 項目 | 說明 |
|---|---|---|
| C1 | 分享頁顯示金額？ | 公開頁不露內部資訊，金額算不算內部需業主決 |
| C2 | 時間軸歷史金額顯示 | `ticket_updates.amount` 已有，詳情可列歷史報價 |
| C3 | vendors 複合索引 | `idx_vendors_active_sort (active, sort_order, id)`——廠商多時優化排序（第九輪） |
| C4 | 時間軸不可變 Trigger | DB 層 Trigger 擋 UPDATE/DELETE ticket_updates，補應用層防線（第九輪） |
| C5 | 逾期 Pill 膠囊 | `.stale` 改淡橘底深橘字小膠囊，比純文字鮮明（第九輪） |
| C6 | assoc IN 子查詢索引 | 確認 `idx_options_type(type, active, sort_order)` 是否覆蓋 `option_categories` 全量覆寫刪除的子查詢（第十輪） |
| C7 | PRAGMA foreign_keys | 測試 `tests/apply-migrations.ts` 未啟用 FK 約束，建議補 `PRAGMA foreign_keys = ON` 以捕捉 FK 違規（第十輪） |

### D. 已施工（第九輪審查後）

| # | 項目 | commit 待填 |
|---|---|---|
| D1 | share 頁底部 70px 空白 | 已修：share.html.ts `<body style="padding-bottom:16px">` |

---

## 第十輪：LINE 生命週期/Schema/前端狀態深度檢視（11 條）核對結果

> 全新維度。多數已做或誤判，真實新發現少。

### 已做（審查誤判/已存在）
| 審查項目 | 現況 |
|---|---|
| §4 options label trim | `label: z.string().trim()` **已做** |
| §6 void 編輯按鈕 | ⋮ 選單已在 `status==='open'||'in_progress'` 才顯示編輯 |
| §2 cleanUrlParams 保存 hash | `pathname + location.hash` **已做** |
| §5 last_activity_at 同一 now | 每 handler 用同一 `nowIso()` **已做** |
| §7 system status NULL | system 寫入 `'system', NULL` **已做** |
| §8 ID 補零溢出 | `padStart(4)` 自動變 5 位，無風險 |
| §9 submit finally | 建單/回報 catch 都重置 `submitting=false`，非卡死 |

### 真實新發現（低風險，列入待辦）
| 審查項目 | 判定 | 對應清單 |
|---|---|---|
| §3 assoc IN 子查詢索引 | 確認 `idx_options_type` 是否覆蓋，低優先 | 新增 C6 |
| §11 PRAGMA foreign_keys 未設 | 測試未啟用 FK 約束，建議補 | 新增 C7 |
| §1 display_name 不同步 | 設計選擇（審查認可），尊重後台改名，不需改 | 不需做 |

### 不需做
- §4 trim（已做）、§8 ID 補零（無風險）、§5 now 一致（已做）、§9 submit（已做）

---
> 施工完成一項，移入 SPEC 版本歷程 v1.1.14 並於本檔標記。
