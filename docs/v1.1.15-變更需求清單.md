# v1.1.15 變更需求清單

> 承接 v1.1.14（已封存至 `docs/archive/v1.1.14-變更需求報告.md`）。本檔只列**尚未施工**的待辦項目，施工完成後移入 SPEC 版本歷程並封存。
> 版本：v1.1.15 ｜ 日期：2026-08-23
>
> **業主拍板紀錄（2026-08-23）**：業主明確指示「拍板全部條目，現在開始照做，遇到問題再說」。本檔所有條目（含「待業主拍板」標註者）視同已授權施工；過程中若發現需要更細設計選擇的條目（F 大項模板渲染、C 大項 LIFF 真機驗證、D2 整表重建等），依「遇到問題再說」原則當場停下、列選項、請業主決策後繼續。

---

## 待辦項目（依優先序，含預計改動方式）

### A. 需業主決策（先確認再做）

| # | 項目 | 預計改動方式 | 來源 |
|---|---|---|---|
| A1 | 統計完成率補充測試驗證 | `month_initial_open` 因現狀快照＋reopen 會失真（SPEC 已註明為設計取捨）。**審查（8/10）指風險**：同月內「done→reopen→再 done」會把案件同時計入分母（現狀 in_progress）與分子（時間軸 done）→ 完成率虛假稀釋。補測試鎖定行為；**更完整解法**：改用 append-only `ticket_updates` 重建「月初時點狀態」而非靠 `tickets.status` 現狀快照 | LIFF 審查 #1 + 全功能審查 A-1 |
| A2 | 建單頁無可用地點時 disabled 提示 | `renderLoc` 在該類別無地點時仍 `disabled=false`。改：無可用地點時保持 disabled 並提示「此類別暫無地點」 | LIFF 審查 #4 |
| A3 | attachPhotoPicker 錯誤處理 | 壓縮失敗（非 NETWORK）被 `continue` 吞掉。改 catch 內一律 `toast(e.message)` | LIFF 審查 #5 |
| A4 | el() 事件名白名單 | 拼錯事件名（如 onfoo）不會即時報錯。開發期加白名單檢查 | LIFF 審查 #6 |
| A5 | 留言/回報後整頁 reload 改局部更新 | 留言/回報提交後 `location.reload()` 重發頁面所有請求。改 `.then(() => router())` 局部刷新時間軸 | 全功能審查 B-4 |
| A6 | 建單時 catalog 重新載入失敗會卡住送出 | 填好的表單若 `ensureCatalog(true)` 剛好過期＋重抓失敗會 throw → 送單卡住。送單路徑對 ensureCatalog 做 try/catch，失敗用既有快取值（頂多選項非最新） | 補充審查 #1 |
| A7 | `app.js` 全域掛載無 module 封裝 | 純 JS 刻意取捨，多人擴編有命名衝突風險。列「長期可維護性」備註，非緊急 | 補充審查 #4 |
| A8 | ~~`wrangler.toml` 的 `database_id` 寫死~~ | **2026-08-23 撤回**。研究後判定 Cloudflare 不支援 `database_id` 以環境變數/secret 取代：① wrangler secret 是 runtime env vars（`env.KEY`），與 `database_id` 那種 build-time binding 不同層；② `[[d1_databases]]` 的 `database_id` 是 schema 必填欄位（wrangler 4.123 cli.js:54202 強制檢查），無 `process.env`/`envsubst` 取代機制；③ 自動 provisioning（不寫 id）會建新 D1、不是取代既有；④ Cloudflare 社群 MVP（WalshyMVP）官方立場：「The DB ID is not a secret, you should just add to the wrangler config.」實際洩漏面已被 schema/migrations/SPEC 全公開包圍，撤回此項。**替代建議**（業主決定後可納 v1.1.16）：若目的是保護 production ID，改從其他真敏感欄位下手（如 `LINE_CHANNEL_ID` 改 secret、`JWT_SECRET` 走 Pages secret 而非 vars）。 | 補充審查 #6 |

### B. 需維運作業（非改碼）

| # | 項目 | 說明 |
|---|---|---|
| B1 | RateLimit | Cloudflare Dashboard 對 share/auth/session/CSV 公開端點設每 IP 限制（SPEC §10）。session 20/min、share 60/min、CSV 10/min（沿自 v1.1.14 未施工） |

### C. LIFF 流程審查（2026-08-23，需真機驗證）

| # | 項目 | 判定 | 預計改動方式 | 施工順序 |
|---|---|---|---|---|
| C1 | `refreshSession()` login fallback 可能迴圈 | 🟠 潛在 bug（中） | `liff.login()` 前加 `loggingIn` flag 防重複觸發；且 login 會整頁重載，`silentRelogin` 判斷到「本次靠 login 重載」時直接回 null 並停止 | 第二階段前端 |
| C2 | `exportCsv()` `liff.openWindow` 無失敗回饋 | 🟡 瑕疵（中低） | 改 `await liff.openWindow(...)` 並 `.catch()` 提示；失敗時 fallback `location.href` | 第二階段前端 |
| C3 | 外部瀏覽器 login 後無二次兜底 | 🟡 弱點（中低） | `boot()` 在 `liff.init()` 失敗時，處理 redirect 回來帶 `code` 的標記 | 第二階段前端 |
| C4 | LIFF SDK CDN（豁免） | ⚪ 非 bug | 在 CLAUDE.md 明確標註「LIFF SDK 為唯一 CDN 例外」 | 文件 |

### D. v1.1.14 暫緩項（移入，未施工）

| # | 項目 | 說明 |
|---|---|---|
| D1 | F6 照片綁定查筆數 | 建單/updates/comments/編輯的 `UPDATE photos AND target_id IS NULL` 不查 `meta.changes`。綁定後檢查筆數，不足回 400 |
| D2 | F8 photos CHECK 約束 | migration 加 `target_type/target_id` CHECK。**需整表重建**，production 風險高 |
| D3 | F7 assoc 半套 | D1 無互動式交易之已知限制。DELETE 全刪＋分批 INSERT 非原子 |
| D4 | C5 逾期 Pill 膠囊 | **2026-08-23 審查通過**。`.stale` 樣式（public/style.css:134）已存在、橘色區隔、已用於列表卡片「⚠ N 天未更新」。清單註明「保留用，無關逾期 Tab」——純 CSS，無需變更。 |
| D5 | C6 assoc IN 子查詢索引 | **2026-08-23 審查通過**。`option_categories` 已有 PRIMARY KEY `(option_id, category_id)`（自動索引）+ `idx_oc_category (category_id)`，兩個查詢方向都有覆蓋。`idx_options_type` 是 `options` 自己的索引，與 `option_categories` 子查詢無關——子查詢走 `option_categories` PK/idx + `options` PK (id)。無需新增索引。 |
| D6 | C8 select 長文字 ellipsis | `.select` 加 `text-overflow: ellipsis` |
| D7 | C9 assoc-list 高度放寬 | max-height 200px→40~50vh |
| D8 | C10 pending 自動輪詢 | pending 頁 `setInterval(checkAuthMe, 5000)` |
| D9 | C11 loading 防抖 | 切 Tab 200ms 延遲顯示 spinner |

### E. 效能

| # | 項目 | 預計改動方式 |
|---|---|---|
| E1 | ~~share 照片端點快取~~ | **2026-08-23 撤回**：與 share-token 撤銷交互風險（快取命中舊 token 資料會洩漏/矛盾）；share 現有照片端點 `Cache-Control: private, max-age=300` 已是合理 TTL；不另做 server-side 快取 |

### F. 統計頁「案件動態」訊息框（2026-08-23 業主需求）

**目標**：保全每天對委員發 LINE 群組報告「今天有變更/新增/完成的案件」。時間口徑為**伺服器台灣時區**任選一天（不限定今日）。訊息依類別切分（每個類別一份），下拉式方塊切換。長度要短（LINE 群組）。新案件詳細、舊案件簡提，附 ticketNo（如 `#0007`）或詳情連結（**登入後才看得到的詳情連結**＝`{baseUrl}/#/ticket/{id}`，**非 share 公開連結**）。訊息格式可自訂（**變數模板**），無更新時顯示**自訂空態訊息**。

#### F1. 後端 — 新增 `GET /api/stats/daily-report`

- **檔案**：`src/routes/stats.ts`
- **權限**：套 `requireAuth()`，三角色皆可（同 `/summary`）
- **Query**：`category_id=N`（**必填**，無 category 不回；不做「全部」避免訊息過長）
- **時間計算**：新增 `src/lib/time.ts → taipeiDayRangeUtc(date?)` 回 `{startIso, endIso}` = 當天 00:00:00.000 ~ **明日 00:00:00.000（半開區間 `[startIso, endIso)`）** 的 UTC ISO；訊息內 `HH:MM` 用 `Intl.DateTimeFormat('zh-TW', {hour:'2-digit', minute:'2-digit', hour12:false, timeZone:'Asia/Taipei'})` 轉
- **回應結構**：
```jsonc
{
  "date": "2026-08-23",                      // 台灣今天
  "category_id": 1,
  "category_label": "水電",
  "total_count": 5,                            // 訊息標題用
  "new_tickets": [{
    "id": 7, "title": "水電－頂樓 #0007",
    "location_label": "頂樓",
    "description": "水泵故障",
    "creator_name": "王小明",
    "created_at": "2026-08-23T...",
    "status": "open",
    "detail_url": "https://repair-system-4re.pages.dev/#/ticket/7"
  }],
  "existing_tickets": [{
    "id": 3, "title": "水電－大廳 #0003",
    "current_status": "in_progress",
    "status_label": "已發包",
    "updates_today": [{                        // 該案件今天所有 update（最多 3 筆）
      "kind": "status", "status": "in_progress",
      "actor_name": "王小明", "time": "10:23",
      "note": null, "amount": 5000
    }]
  }]
}
```
- **SQL 策略**（避免 N+1）：
  1. **新建**：`SELECT ... FROM tickets t JOIN users u ON u.id=t.created_by WHERE t.category_id=? AND t.created_at >= startIso AND t.created_at < endIso`，JOIN 拿 creator_name
  2. **既有**：`WHERE category_id=? AND t.last_activity_at >= startIso AND t.last_activity_at < endIso AND t.id NOT IN (新建 ids)`——**包含純留言案件**（委員留言也算「提醒」內容，業主 2026-08-23 決策）
  3. **updates_today**：用 `SELECT u.*, usr.display_name FROM ticket_updates u JOIN users usr ON usr.id=u.user_id WHERE u.ticket_id IN (既存 ids) AND u.created_at >= startIso AND u.created_at < endIso ORDER BY u.created_at ASC` → 應用層按 ticket_id 分組 + slice(0,3)（**DESC LIMIT 是撈最新 3 筆用，最終輸出按 F12-1 時間正序**）
- **detail_url**：用 `env.BASE_URL`（沒設 fallback `https://repair-system-4re.pages.dev`） + `/#/ticket/${id}`；不走 share_token（業主明確要登入後才看得到的連結）
- **契約變更**：新增 endpoint，**不影響**既有 `/summary`、`/amount-by-category`——純加

#### F2. 日期選擇器（業主決策 2026-08-23：任選一天）

- **`daily-report` 接收 `date=YYYY-MM-DD`**（**必填**，無 date 不回；前端預設填今天，使用者改日期即重抓）
- 後端 `taipeiDayRangeUtc(date)` 接收指定日期 → 回 `{startIso, endIso}`，startIso=該日 00:00:00.000 UTC、endIso=**明日** 00:00:00.000 UTC（半開區間）
- 前端 `<input type="date">`，max=今天（不允許選未來），onchange 重抓
- **不做**今日/昨日/本週三選一，**單純日期選擇**就夠業主用了

#### F3. 前端 — 統計頁新增「案件動態」訊息框

- **檔案**：`public/app.js → pages.stats()`
- **位置**：「各類別金額」區塊下方，不擠壓現有版面
- **結構**：
  ```
  [月份下拉]                         ← 既有
  [六張數字卡]                       ← 既有
  [各類別金額區塊]                   ← 既有
  ───── 案件動態 ─────                 ← 新增區塊
  [日期 <input type="date">]          ← 新增（預設今天，max=今天）
  [類別下拉]                         ← 新增（從 ensureCatalog() 拿 categories）
  [📋 複製] 按鈕                      ← navigator.clipboard.writeText()，fallback execCommand
  [訊息預覽 textarea readonly]        ← 依模板即時渲染
  ```
- **訊息渲染方式**：依 `daily-report` 回傳的資料 + 從前端抓的「啟用模板」→ 用模板變數替換 → 渲染到 textarea（純函式，可單測）
- **類別下拉**：預設第一個類別，**不做「全部類別」選項**（訊息會過長），**不存 hash**（避免把作業現場帶進 URL）。**定案**：用 `localStorage` 記住上次選擇（業主 2026-08-23 決策：保全每天開統計頁是看昨日類別，記住更實用；刷新頁面沿用上次的類別而非重置）
- **複製按鈕**：navigator.clipboard.writeText；LIFF WebView / iOS Safari 權限問題 fallback `document.execCommand('copy')` + toast「已複製」

#### F4. 空態訊息自訂（業主決策 2026-08-23）

當 `total_count === 0`（該類別該日無任何更新）時，**不走任何模板**，直接顯示一條「無更新訊息」：

- **預設空態**：`今日 {date} {category_label} 無案件動態`
- **業主可在模板管理自訂**：「無更新訊息」**也是模板的一種**，type 區分為 `empty`，只有一個啟用（見 F7 schema）
- 訊息框在無更新時仍顯示訊息+複製按鈕（讓保全照樣貼 LINE 群組）

#### F5. 訊息變數模板系統（業主決策 2026-08-23）

業主可在「訊息模板管理」頁（manager/admin）**自訂訊息模板**，用 `{{變數名}}` 塞資料。

**可用變數（固定，與 API 回應欄位一一對應）**：

| 變數 | 來源 | 範例 |
|---|---|---|
| `{{date}}` | API 回傳 | `2026-08-23` |
| `{{category_label}}` | API 回傳 | `水電` |
| `{{total_count}}` | API 回傳 | `5` |
| `{{new_count}}` | 計算 | `2` |
| `{{existing_count}}` | 計算 | `3` |
| `{{new_tickets}}` | 渲染：迴圈區段（見下方「區段語法」） | （多行） |
| `{{existing_tickets}}` | 渲染：迴圈區段 | （多行） |

**迴圈區段語法**（**極簡**，不引入 jinja/template engine）：
```
{{#each new_tickets}}
  {{序}}. {{title}}
     {{description}}
     詳情：{{detail_url}}
{{/each}}
```
- `{{#each X}}...{{/each}}` 區段會對陣列每筆展開
- 區段內可用該筆的所有欄位（id, title, location_label, description, creator_name, created_at, status, detail_url）
- `{{existing_tickets}}` 區段內額外可用 `{{#each updates}}...{{/each}}` 巢狀迴圈，每筆 update 有 time / actor_name / note / status / amount / kind

**預設模板**（**全預設寫死**，業主不編輯也能用）：
```
📅 {{date}} {{category_label}}案件動態（共 {{total_count}} 件）

{{#if new_count>0}}🆕 新建案件（{{new_count}}）：
{{#each new_tickets}}
{{序}}. {{title}}
   {{description}}{{creator_name}} {{created_at_time}}
   詳情：{{detail_url}}
{{/each}}{{/if}}

{{#if existing_count>0}}📝 既有案件更新（{{existing_count}}）：
{{#each existing_tickets}}
{{序}}. {{title}} → {{status_label}}
{{#each updates_today}}
   {{time}} {{actor_name}}：{{note_or_status}}{{amount_text}}
{{/each}}{{/each}}{{/if}}
```

**注意**：上面的 `{{#if X>0}}` 條件語法是「極簡示意」，實際實作 v1.1.15 只支援 `{{#each}}...{{/each}}` 一種控制語法，**不做 if 條件**（太複雜）。改用「空陣列不渲染」自動處理：若 `new_count===0`，`{{#each new_tickets}}...{{/each}}` 區段整段消失。

**邏輯前提**：`existing_tickets` 是用「`last_activity_at` 落在當日」撈出，所以**每筆 existing_ticket 必至少 1 筆 update**（否則 `last_activity_at` 不會被更新）；因此「`{{#each existing_tickets}}` 內 `{{#each updates_today}}` 空陣列」**不會發生**，預設模板不需額外防呆。

#### F6. 後端 — 模板 CRUD

- **新 migration 0010**（F12-2：沿用既有 `options` 字典表 + `option_categories` 關聯表，**不新開表**）：
  ```sql
  ALTER TABLE options ADD COLUMN body TEXT;

  -- 預設模板：type='message_template'，label 區分子類型（report/empty）
  -- 不寫 option_categories = 全域預設（通用 fallback）
  INSERT OR IGNORE INTO options (type, label, sort_order, active, body) VALUES
    ('message_template', 'report', 0, 1, '📅 {{date}} {{category_label}}案件動態（共 {{total_count}}件）...'),
    ('message_template', 'empty',   1, 1, '今日 {{date}} {{category_label}} 無案件動態');

  CREATE INDEX IF NOT EXISTS idx_options_message_template
    ON options(type, label, active) WHERE type = 'message_template';
  ```
- **類別關聯**：用既有 `option_categories` 多對多表（**不動 categories 表**）——例：`category_id=3` 設自訂報告模板 → INSERT INTO option_categories(option_id=Y, category_id=3)
- **路由**（`src/routes/messageTemplates.ts`）：
  - `GET /api/message-templates?category_id=N` — 三角色可讀；JOIN option_categories 回**該類別關聯的模板** + 無關聯的 default 兜底
  - `GET /api/message-templates/:id` — 三角色可讀
  - `PUT /api/message-templates/:id` — manager/admin，編輯 body/label（**唯一改模板方式**，F12-2）
- **seed 預設**：上面 INSERT 已包含兩個預設（report/empty）
- **`daily-report` 改為**：原本的內建組訊息邏輯**刪除**，改為回傳純資料 + **前端**用模板渲染（避免後端組兩次）
  - 後端仍可加 `?template_id=N`（缺省用 active 的 report 模板），但**不渲染**，只回 `template.body` 讓前端用，**或**前端另呼叫 `/api/message-templates` 抓 active（兩種都可，前者省一次 roundtrip → 推薦）

#### F7. 前端 — 訊息模板管理頁（manager/admin）

- **位置**：成員管理（pages.users）旁加一個 tab 「訊息模板」→ 新 `pages.messageTemplates()`
- **權限**：manager/admin 可編輯（committee 看不到此頁——**F11-1 已定**放管理內、不從 nav 進）
- **UI**：
  ```
  [Tab: 報告模板] [Tab: 無更新訊息]
  ────────
  [名稱: 報告模板（最新）]      [編輯]
  [名稱: 無更新訊息模板（最新）] [編輯]
  ────────
  ```
  每個 Tab 永遠**只列一筆**（該 type 的 `active=1`）——管理者、保全**不會看到歷史紀錄**，避免搞混
- **使用者層互動（極簡版）**：
  - **沒有「啟用」按鈕**（每 type 只一筆 active，沒有切換問題）
  - **沒有「新增」按鈕**（編輯就是修改該筆 body，存檔後**該筆就生效**）
  - **沒有「刪除」按鈕**（`is_default=1` 模板不可刪；非預設但 active=1 的不能刪，因為「編輯就是改」）
  - **只留「編輯」**：點下去改 body → 存檔 → 直接覆寫該筆 → 重抓列表
- **資料庫層（內部留底）**：
  - 每次編輯**不刪舊 body**，保留完整歷史（透過 `active=1` vs `active=0` 區分）
  - 部分唯一索引 `UNIQUE(type, active) WHERE active=1` 確保每 type 同時間只一筆 active
  - ~~F12-2 後改用既有 options 字典表，本段作廢——options 沒有歷史，DB 留底不適用~~
  - 業主日後想查「過去改了什麼」→ 直接 SQL 撈 `WHERE type='report' ORDER BY updated_at DESC`
- **編輯 modal**：
  - 標題（name，input）
  - 內容（body，textarea，**等寬字體** monospace）
  - 下方**即時預覽**：用當前測試資料（hardcoded fixture）跑模板引擎渲染 → 顯示「預覽」框
  - **變數快速插入（業主決策 2026-08-23）**：以下三種方式組合，讓業主不必記變數名
    1. **下拉式方塊（頂部）**：編輯框上方一排兩個 `<select>`，左選變數類型（如「頂層變數」「迴圈區段」「巢狀欄位」），右選該類下的具體變數（如 `{{date}}` `{{total_count}}` `{{new_tickets}}`），選好後點「插入」或按 Enter → 變數字串插入到 textarea 游標位置
    2. **點擊插入面板（右側）**：列出所有可用變數，點擊直接插入游標位置（與 v1.1.13「使用範本」同樣 UX）
    3. **自動完成（textarea 內）**：在 textarea 輸入 `{{` 觸發彈出選單，鍵盤上下選 + Enter 確認插入（與 IDE IntelliSense 同 UX，可選但強烈建議做）
  - **下拉方塊分層設計**：避免「100 個變數擠在一個 select」的災難，分三層
    - 第一層：「頂層變數（8 個）」「new_tickets 迴圈欄位（8 個）」「existing_tickets 迴圈欄位（5 個）」「existing_tickets → updates_today 巢狀迴圈欄位（6 個）」「自動變數（4 個）」「控制語法（2 個：each）」
    - 第二層：依類型列出該組變數
    - 第三層：選中後插入的字串（含 `{{}}` 包裝），含 1 個完整 `{{#each}}...{{/each}}` 範例文字（cursor 在中間）
  - **插入游標行為**：保留游標位置，**不覆蓋選取範圍**（已選取的文字不被變數字串取代，而是把變數插在選取起點）
- **啟用流程**：**不做啟用**（每 type 只一筆 active，無需切換）。若需要更換模板內容，**直接編輯該筆** → 後端 `PUT /:id` → 該筆 body 覆寫 → 前端重抓列表

#### F8. 前端 — 模板渲染引擎（純函式，可單測）

**新檔**：`public/templateEngine.js`（或合併進 app.js，**新檔較好測**）：

```js
// 變數替換：{{key}} → 從 ctx 物件取值
// 區段展開：{{#each arr}}...{{/each}} → 對 arr 每筆展開
// 巢狀支援：{{#each outer}}...{{#each inner}}...{{/each}}...{{/each}}
// 變數點查找：先查 ctx 本身，找不到查當前 each 迴圈的 item
// 缺值：保留 {{key}} 字面（讓使用者知道漏變數）；額外 console.warn

export function render(template, ctx) { /* ... */ }
```

**輸出邊界**：
- `{{序}}` 在 each 區段內自動填 `1, 2, 3...`（不是 ctx 欄位，是 engine 自動加的計數器）
- `{{created_at_time}}` 不是直接欄位，是 `created_at` ISO 轉 `HH:MM`（engine 自動轉，台灣時區）
- `{{note_or_status}}` 在 each updates 內：`kind='comment'` 用 `note`，否則組「狀態：{status_label}」
- `{{amount_text}}` 在 each updates 內：amount 非空時拼「（$5,000）」

**單元測試**（`tests/templateEngine.test.ts`）：
- 簡單變數替換
- each 區段展開（含空陣列）
- 巢狀 each
- 缺變數保留 `{{key}}`
- 特殊字元 escape（用戶模板可能含 `<` `>` `&`，**不做 HTML escape**，訊息是純文字）
- **多行片段插入渲染**（F7 下拉插入 each 範例對應）：驗證完整 `{{#each updates_today}}\n   {{time}} {{actor_name}}\n{{/each}}` 多行模板（含 `\n`）經引擎渲染後**正確保留換行**且每筆 update 完整展開——避免「UI 能插進去但引擎不會渲染多行」的施工期炸 case

#### F9. 規格同步

- **SPEC §4.7**：新增 endpoint `GET /api/stats/daily-report?category_id=N&date=YYYY-MM-DD&template_id=N` 完整定義
- **SPEC §4.x（新章節）**：模板 CRUD endpoints + 模板語法定義 + 變數清單
- **SPEC §5.5**：加「案件動態」區塊描述（訊息框、日期選擇器、類別下拉、複製按鈕）
- **SPEC §5.x（新章節）**：訊息模板管理頁
- **SPEC 版本歷程**：加 v1.1.15 變更內容（**含模板系統**這項新功能）
- **CLAUDE.md**：補三條規則：
  1. 訊息預覽顯示時間一律台灣時區（與 SPEC §8.5 一致）
  2. 詳情連結走登入後路由 `/#/ticket/{id}`，**不走 share_token**
  3. **模板變數語法**：`{{key}}` 替換 / `{{#each array}}...{{/each}}` 迴圈；後端**不渲染模板**（純資料 + template.body），前端負責渲染
- **test-cases.md**：補測試案例
  - 後端：`daily-report` 跨日不混（台灣 23:00 建的單算當天）、其他類別不出現、無更新回空
  - 後端：模板 PUT 編輯（**無 CRUD、無 activate**）+ 部分唯一索引作用驗證（F12-2）
  - 前端：渲染引擎（純函式）— 變數替換、each 展開、巢狀、缺值
  - 前端：模板管理 CRUD UI（mock）
  - 前端：統計頁訊息框（mock）
- **README.md / lib-spec.md / page-api-map.md**：版本同步 v1.1.15、page-api-map 加 daily-report + message-templates

#### F10. E2E

- 新增 `e2e/daily-report.spec.js`：
  - 開 `?mock=true#/stats`
  - 改日期 input，驗證訊息預覽更新（mock 不同日回不同 fixture）
  - 切換類別下拉，驗證訊息預覽更新
  - 點複製按鈕，驗證 clipboard 內容
  - 切到無更新的類別，驗證顯示預設空態訊息
- 新增 `e2e/message-templates.spec.js`：
  - manager 進 `#/message-templates`（若無此路由加空頁）
  - 編輯模板、改 body、存檔
  - 回到統計頁驗證新模板生效（**F12-2：無啟用步驟**）
- mock 補強（`mockApi`）：
  - `/api/stats/daily-report` handler：回固定 fixture（1 新建 + 2 既有 + 1 無更新類別）
  - `/api/message-templates*` 系列 CRUD handler
  - **mock 模板至少 2 套**：「簡潔（預設）」「詳細版」，讓 E2E 驗「切模板」效果

#### 範圍邊界（明確不做）

- ❌ 多日切換捷徑（今日/昨日/本週）— 純日期選擇器已足
- ❌ 自動發到 LINE 群組（只在 App 內顯示+複製）
- ❌ 廠商姓名（資料流未含）
- ❌ 「全部類別」選項（訊息過長）
- ❌ **模板 if/else 條件語法**（只做 each 迴圈）
- ❌ **模板巢狀迴圈內的 if/過濾**（不支援 `{{#each updates where kind='comment'}}`）
- ❌ HTML escape（訊息是純文字，LINE 端會自動處理）
- ❌ 模板版本歷史（編輯後舊版直接消失）
- ❌ 模板 import/export
- ❌ 多語系模板（目前繁中即可）
- ❌ 編輯訊息後再複製（textarea 唯讀）

#### 風險與卡點

1. **訊息長度**：LINE 群組單則上限 5000 字元；模板內建每筆 update 最多 3 筆的限制由模板作者決定（不再由程式強制），需在預設模板註解提醒
2. **跨午夜 created_at**：指定 `date=2026-08-23` 時，台灣 02:00 建單（UTC 前日 18:00）算 2026-08-23（**符合語意**：保全在 8/23 下班前看 8/23 整天）→ 後端 `taipeiDayRangeUtc('2026-08-23')` 必須正確處理
3. **多次結案的同案件**：done→reopen→再 done 在指定日內 updates 都列，模板作者需自己用 `{{#each}}` 控制
4. **`last_activity_at` 可靠性**：每次 create update/void/reopen 都會更新，但施工前需查證寫入路徑全數更新
5. **模板引擎安全性**：用戶自訂模板含 `{{}}` 必須嚴格解析，不可誤執行為 JS；**純字串替換** 不會有此風險
6. **active 唯一性**：SQLite `UNIQUE INDEX ... WHERE active=1` 是部分索引，D1 支援——**保證每 type 同時間只一筆 active**。F12-2 採「使用者層無啟用」設計，**沒有 `/activate` 路由、無 transaction 競態風險**（只有 PUT 編輯 body 與 active 旗標無關）
7. **預設模板保護**：`is_default=1` 不允許刪除（前端禁用按鈕，後端 403 雙重防護）

#### 施工順序（此需求）

1. **後端 — 模板系統**：`src/routes/messageTemplates.ts` **只做 PUT 編輯**（F12-2：無 CRUD、無 activate、無 delete）→ migration 0010（含預設模板 seed）→ typecheck + 單元測試
2. **前端 — 模板引擎**：`public/templateEngine.js` 純函式 + 完整單元測試（**最關鍵**，後面全靠它）
3. **前端 — 模板管理頁**：`pages.messageTemplates()` CRUD UI + 變數面板 + 即時預覽（用第 2 步引擎）
4. **後端 — daily-report**：`taipeiDayRangeUtc(date)` helper → `/api/stats/daily-report?category_id=&date=&template_id=` handler（**只回資料 + template.body**，不渲染）→ typecheck + 單元測試
5. **前端 — 統計頁訊息框**：`pages.stats()` 加區塊 + 日期 input + 類別下拉 + 套模板引擎渲染 + 複製按鈕
6. **E2E**：mock 補強 + daily-report spec + message-templates spec
7. **文件**：SPEC §4.7 §4.x §5.5 §5.x 版本歷程 + CLAUDE + test-cases + lib-spec + page-api-map + README
8. **push + CI**（靠 GitHub Actions）

#### F11. 審查第三輪整合（2026-08-23）

> 整合審查者第三輪提出的 5 點新增發現 + 上一輪已接受但檔案尚未修正的 2 點遺漏 + 1 點衍生。業主拍板：審查意見可直接整合進計畫，不必逐項請示。

##### F11-1 🔴 訊息模板入口放「管理」頁內，不加底導

- **入口位置**：`pages.messageTemplates()` 與 `pages.admin` 同層級，**從管理頁進**；底部導覽 `#nav` 不動
- **理由**：與既有 `pages.users`（限 admin）一致，committee 是「接收者」不是「管理員」，不需看到入口
- **路由**：hash router 加 `#/message-templates` 分派 → 進 `pages.messageTemplates()`
- **路由分派順序（重要）**：`#/message-templates` **必須在 `#/admin/:type` 之前註冊**——`pages.admin` 是變數型路由（內部用 `types` 陣列切 tab），若順序顛倒，`#/message-templates` 會被 `:type` 模式誤匹配吞掉，路由失敗（白屏）
- **新增 helper（時點）**：F11-2 提及的 `taipeiToday()` 應一併補進 lib-spec；若 `time.ts` 已有則沿用
- **committee 體驗**：committee 看不到入口（nav 與 admin 頁都不列），若有人把網址丟給他：進得去但只有預覽、CRUD 按鈕全隱藏
- **權限矩陣**：
  - admin：看得到入口、CRUD 全開
  - manager：看得到入口、CRUD 全開
  - committee：看不到入口、直接打網址也只讀不可編
- **F7 文件同步**：F7 段落**改寫**為「位置：從 `pages.admin` 進入的子頁（非 nav 第一層）」

##### F11-2 🔴 後端 `daily-report` 必須擋 `date` 真日期 + 不得晚於台灣今天

- **後端驗證**：`date` 必填、用 **`exports.ts:9` 的 `dateStr` schema**（**不是 `stats.ts` 的 `month` zod**——一個是 `YYYY-MM-DD` 一個是 `YYYY-MM`，不可互套）
- **規則**：
  1. `date` 缺失 → 400 `MISSING_DATE`
  2. `date` 不是 `YYYY-MM-DD` 格式或不是真日期（如 `2026-99-99`、`2026-02-30`）→ 400 `INVALID_DATE`
  3. `date` 晚於台灣今天（`taipeiToday()`）→ 400 `DATE_FUTURE`
  4. `date` 早於系統最早案件日（**不擋**，允許查空集）
- **前端** F2 的 `<input type="date"> max=今天` 是 UX 友善，**不能取代**後端驗證（curl 可繞）
- **對齊慣例**：`stats.ts:2-3` 對 `month` 有嚴格驗證，`daily-report` 對 `date` 採同樣嚴謹度
- **既有同名 schema 不可直接套**：`stats.ts` 的 `month` zod 是 `YYYY-MM` 格式，`exports.ts:9` 的 `dateStr` 才是 `YYYY-MM-DD`，**必須用後者**
- **`taipeiToday()` helper 確認/新增**：規則 3 需與台灣今天比對，若 `time.ts` 已 export `taipeiToday()` 則沿用，否則本次新增（回 `YYYY-MM-DD`，用 `taipeiDayRangeUtc()` 內部推導或單獨實作）。**lib-spec §time.ts 補一條**，文件同步 F9

##### F11-3 🟠 F6 seed body 必須是「去掉 if 的乾淨 each-only 版」

- **根因**：F5 預設模板 body 還帶著 `{{#if new_count>0}}` / `{{#if existing_count>0}}` 與 `{{/if}}`，但 F8 引擎只支援 `{{#each}}...{{/each}}`，遇到 `{{#if}}` 會當未知變數保留字面 → **第一次載入就是髒的**
- **施工規定**：F6 seed 的預設模板 body **必須是 F5 預設內容去掉所有 `{{#if X>0}}` / `{{/if}}` 後的版本**，並靠「空陣列不渲染」自然達成條件效果
- **F5 文件更新**：F5 預設模板區塊後**加註「以下為示意，seed 實寫時去掉 if」**，避免施工者照抄
- **預設模板 seed 最終樣**：
  ```
  📅 {{date}} {{category_label}}案件動態（共 {{total_count}} 件）

  {{#each new_tickets}}
  {{序}}. {{title}}
     {{description}}｜{{creator_name}} {{created_at_time}}
     詳情：{{detail_url}}
  {{/each}}

  {{#each existing_tickets}}
  {{序}}. {{title}} → {{status_label}}
  {{#each updates_today}}
     {{time}} {{actor_name}}：{{note_or_status}}{{amount_text}}
  {{/each}}{{/each}}
  ```

##### F11-4 🟠 `note_or_status` 補 `system` 第三態分支

- **根因**：`ticket_updates.kind IN ('comment','system')` 時 `status IS NULL`（CHECK 約束）；F8 `note_or_status` 只處理 `comment` vs 其他，**沒處理 `system` 與 `status` 的差別**
- **既有案件當日若被編輯過**：會留 `kind='system'` 的 system update，`status=NULL`，套既有規則會組出「狀態：」（空狀態標籤）
- **修正**：F8 `note_or_status` 三分支邏輯：
  - `kind='comment'` → 直接用 `note`
  - `kind='status'` → 組「狀態：{status_label}」
  - `kind='system'` → 用 `note`（編輯留痕的系統訊息），**不顯示狀態**
- **F1 SQL 補充**：`updates_today` SQL 撈全部 kind，**不過濾**——系統留痕也算當日動態（業主：編輯也代表有動作）

##### F11-5 🟡 F1 回應結構加 `template.body` 欄位

- **F1 回應結構補欄位**：
  ```jsonc
  {
    "date": "...",
    "category_label": "...",
    "total_count": 5,
    "template": {                              // ← 新增
      "id": 1,
      "body": "📅 {{date}}..."                // 模板原始 body
    },
    "new_tickets": [...],
    "existing_tickets": [...]
  }
  ```
- **total_count=0 時**：回 `template.body` 為 **active 的 `empty` 模板 body**（不是 report 模板）
- **前端渲染來源**：直接用回應裡的 `template.body`，**不必另抓** `/api/message-templates`（省 roundtrip）
- **F6 `daily-report` 改為段落同步更新**：原「只回 `template.body`，或前端另抓」的二選一文字**刪除**，統一為「**回應必帶 `template.body`**」

##### F11-6 🟠 上一輪已接受但檔案未修 — F1 SQL「既有」漏 `d`

- F1 SQL 第 2 條**既有**查詢**改為**：
  ```sql
  WHERE t.category_id=? AND t.last_activity_at >= ? AND t.last_activity_at < ? AND t.id NOT IN (...新建 ids...)
  ```
  上一輪文字只寫 `category_id`，**補上時間過濾條件**；本版依 F11-7 統一改半開區間 `>= AND <`，不再 BETWEEN（F11-6/F11-7 同步）

##### F11-7 🟠 上一輪已接受但檔案未修 — `23:59:59.999` 反模式改半開區間

- **根因**：`23:59:59.999` 的 `.999` 在浮點時間戳與某些 DB 時間欄位會被四捨五入掉，**半開區間 `[start, nextDay00:00:00)`** 才是業界慣例（PostgreSQL、D1、SQLite 都用此模式）
- **F1 `taipeiDayRangeUtc(date?)`** 改為：
  ```ts
  // 回 {startIso, endIso}，endIso 是「明日 00:00:00.000 UTC」（半開區間）
  export function taipeiDayRangeUtc(date?: string): { startIso: string; endIso: string }
  ```
- **SQL 改為**：`WHERE t.created_at >= startIso AND t.created_at < endIso`（**不再用 BETWEEN**）
- **F2 文件同步**：F2 寫「00:00:00.000~23:59:59.999 UTC」的文字**改為**「`[startIso, endIso)` 半開區間，endIso 是明日 00:00:00.000 UTC」
- **F1 所有時間過濾統一改用 `>= AND <`**：本條不只限 F1 示範 SQL，**F1 中所有時間過濾（新建 `t.created_at`、既有 `t.last_activity_at`、`updates_today.created_at`）統一 `>= startIso AND < endIso`**，不再 BETWEEN、不寫 `.999`

#### F12. 審查第三輪整合 B（2026-08-23）

> 業主拍板：審查意見可直接整合進計畫，不必逐項請示。

##### F12-1 🟠 `updates_today` 排序寫死

- **排序規則**：`updates_today[]` **時間正序**（舊的在最上、新的在最下）——閱讀訊息時符合「先看早的、再看晚的」直覺
- **SQL**：`ORDER BY u.created_at ASC`（DESC 是撈最新 3 筆用的，LIMIT 完後程式端**不可再排序**——避免雙重排序出錯）
- **訊息模板呈現**：因 each 區段按陣列順序輸出，正序 = 早的先出、晚的後出，閱讀順
- **若業主改主意要倒序**：本條單獨反轉即可，不影響其他

##### F12-2 模板管理「用既有 options 字典表」（2026-08-23 業主決策）

- **核心發現**：`options` 表早已是「**泛用選項字典**」設計（`type` 區分用途，現有 `category`/`location`/`description` 共用），加上 `option_categories` 多對多關聯表，**這套架構正是為「模板 + 類別關聯」場景而生**——**不必新開表**
- **儲存設計**：
  - 模板用 `options.type='message_template'` 區分（與既有 `description` 範本同模式）
  - 每個 template 子類型用 `label` 區分（`report` / `empty`）
  - migration 0010 只做一件事：**`ALTER TABLE options ADD COLUMN body TEXT`**
  - 類別關聯走既有 `option_categories(option_id, category_id)`——**不動 categories 表**
- **default fallback 機制**：
  - 「全域預設模板」= **無 `option_categories` 關聯的那筆**（與既有「無 option_categories = 通用、所有類別可見」慣例完全一致）
  - 類別有設關聯 → 用關聯；沒設 → 用 default（**前端 SQL 一次撈就解決**：LEFT JOIN option_categories）
- **使用者層（簡化版）**：
  - 每個 template 子類型 Tab（`report` / `empty`）永遠**只顯示一筆**：該類別關聯到的模板，若無關聯則顯示全域預設
  - **只有「編輯」按鈕**——點下去改 body → 存檔 → 直接覆寫 → 該筆生效
  - **沒有啟用/新增/停用/刪除/歷史**——管理者看到太多選項會搞混
- **DB 留底**：options 表無歷史（key-value 屬性），**本次設計直接放棄 DB 留底**——你親口選這條路，跟「使用者層無歷史」一致即可
- **F6 同步取消新建表**：原規劃的 `CREATE TABLE message_templates(...)` + 部分唯一索引 + 7 欄 schema **全部移除**，改為 ALTER TABLE options ADD COLUMN body
- **F7 UI/路由同步重寫**：modal 只剩「編輯」、列表永遠一筆、`PUT /:id` 為唯一改模板方式（保持原本規劃）
- **契約變更**：原本 F7 規劃的 `POST /:id/activate` 路由**移除**（無啟用概念）；`POST` 新增也移除（編輯即修改）

#### F13. 目前進度快照（2026-08-23，準備交接包）

> 業主指示：程式開始不穩定一直跳出 → 暫停施工、整理進度、準備交接包。

**已完成討論、F11/F12 文件整合完成**：
- F1-F10：案件動態訊息框完整功能設計（訊息模板 CRUD、daily-report API、統計頁區塊）
- F11（第三輪審查整合 A）：F11-1~7 共 7 點（路由順序、date 驗證、seed 乾淨化、note_or_status system 第三態、F1 回應加 template.body、SQL 半開區間等）
- F12-1：`updates_today` 排序寫死時間正序（ASC）
- F12-2：**模板管理簡化**——不開新表、用既有 `options` 字典表 + `option_categories` 關聯表
  - migration 0010 只 `ALTER TABLE options ADD COLUMN body TEXT` + seed 兩筆預設
  - 每 type 永遠只顯示一筆、只有「編輯」按鈕
  - 「全域預設」= 無 `option_categories` 關聯的那筆（與 `description` 範本同模式）

**尚未施工、文件已備齊可由下一位接手直接動工**：
- F1 後端 `GET /api/stats/daily-report`：template.body 欄位、半開區間、date 驗證
- F6 migration 0010：ALTER options + seed + 索引
- F7 前端 `pages.messageTemplates()`：簡化版 UI（只編輯、列表一筆）
- F8 前端 `public/templateEngine.js` 純函式渲染引擎
- F9 規格同步（SPEC / lib-spec / page-api-map / test-cases / CLAUDE）
- F10 E2E（`daily-report.spec.js` + `message-templates.spec.js`）

**接手者注意事項**：
- **工作區目錄**：`/var/minis/workspace/repair-system/`（**非 `/tmp/repo-work`**——本版已完成統一，git 在 workspace 內可運作，但須注意 `.git/objects/` 內檔案不可 `rm`、跨 l2s 路徑不可 `mv`，詳見 2026-08-23 工作區統一筆記）
- **本版所有 commit** 皆已 push 至 `main`（遠端 asdf80730/repair0818）
- **未 commit 的 SPEC.md 變更**（v1.1.14 既有追補：規則 9-15）保留在工作區、未推送，**由接手者決定如何處理**
- **施工順序建議**：F6 migration（後端、含 seed）→ F8 渲染引擎（純函式、可單測）→ F7 模板管理頁 → F1+F4 後端 daily-report → F3+F4 前端統計頁 → F10 E2E → F9 文件 → push+CI
- **常見踩雷**（本版發現）：
  1. **mock 模式不走瀏覽器 fetch**：`api()` 開頭 `if (IS_MOCK) return mockApi()`，`page.on('request')` 攔不到，E2E 只能驗 UI 互動
  2. **batch + INSERT...SELECT...WHERE EXISTS 有 bug**：batch 依序執行，UPDATE 先改狀態、EXISTS 讀到新狀態，永遠 0 筆 → 400。改兩步寫入
  3. **`.git/objects/` 內檔案不可 `rm`**：l2s 同步層鎖死，會導致 `git gc` 等操作失敗
  4. **shell `mv` 跨 l2s 路徑會壞**：`rename(2)` + `unlink(2)` 兩階段，unlink 被擋；改用 `git mv` 或 cp
  5. **D1 部分唯一索引語法**：先在沙箱測，SQLite `WHERE active=1` 語法 D1 支援但需驗證

**審查者信心指數紀錄**：本版經四輪審查（v1.1.15 三輪 + 本輪 options 化），目前文件自一致性約 9/10（已修正 F11-6/F11-7 BETWEEN 矛盾、F1 簽名統一、seed body 加分隔、F12-2 三次路由/E2E 對齊）。**最後一個未驗證項**：C1-C3 需 LINE 真機測試。

**交接包範圍**（下一步）：含 git 工作區、變更清單、CLAUDE、SPEC、migrations、原始碼全部 commit 範圍。

### G. 收尾雜項（2026-08-23 業主指示）

| # | 項目 | 預計改動方式 | 狀態 |
|---|---|---|---|
| G1 | 清理沒用到的程式碼片段或檔案 | `tsc --noUnusedLocals` 已乾淨；E2E mock fixture 與正式契約同步；SPEC §0.3「不做」清單與實際一致 | 審查中，待施工 |
| G2 | 清理重複嘗試多裝的軟體 | 沙箱端 `pip`/`apk` 套件審查；非專案範疇，列備忘（**沙箱工具不影響 production**） | 不施工 |
| G3 | 規格更新到現有階段 | SPEC 版本標頭已升 v1.1.14、README/lib-spec/test-cases 已同步；本 v1.1.15 清單即「現有階段需更新的內容」 | 進行中 |
| G4 | 暫存檔清理 | `/tmp` 暫存 log（ci*.log、e2e.log 等）與失敗的 git clone 殘留（repair-fresh-clone、repair-git-broken2）可刪；正式施工目錄統一決策見 G5 | 待施工 |
| G5 | 正式施工目錄統一 | **業主決定**：正式施工目錄統一在 `workspace/repair-system/`；其他 `/tmp` 下的 clone 清掉。**踩雷（已實測 2026-08-23）**：workspace 下的 `.git` 會被 Minis l2s 同步層鎖死（連 `rm` 都 `Operation not permitted`），`--separate-git-dir` 仍會被鎖。**解決方案：採用方案 C**——不論施工在哪，做「收尾 SOP」：完工自動把最新源碼**快照**（無 .git，純檢視）同步到 `workspace/repair-system/repair-system-snapshot/`，清 `/tmp` 多餘 clone，跑 CI。git 實體留在 `/tmp/repo-work`，但工作區永遠一致 | 待業主確認方案 |
| G6 | 變更報告歸檔 | v1.1.15 全部施工完成後，`docs/v1.1.15-變更需求清單.md` 移入 `docs/archive/v1.1.15-變更需求報告.md`。**不在當下歸檔**——業主剛批准計畫還沒施工，動歸檔會把計畫連同活清單一起搬走，違反慣例（參考 v1.1.14 流程） | 完工後執行 |

#### G5 三方案比較（業主 2026-08-23 已看過）

- **方案 A**：git 維持 `/tmp/repo-work`，workspace 當「最新快照」（每次完工同步）→ **推薦**
- **方案 B**：workspace 用，但每次改完 cp 回 /tmp 的 git repo 再 push → 易出錯
- **方案 C**：靠 SOP 自動同步 → **同 A，本質一樣**

| G7 | 預設模板「重置出廠預設」按鈕（提案） | 範圍邊界「不做版本歷史」，但 `is_default=1` 模板可被編輯覆寫、無還原機制。**提案**：模板管理頁加「重置為出廠預設」按鈕（把 seed body 寫回），低成本高價值避免誤改無法還原。**不在本 v1.1.15 範圍**——待業主決定是否納 v1.1.16 | 待業主拍板 |

---

## 施工順序（相依性最佳化）

1. **第一階段：測試基建** — A1（完成率測試＋reopen 失真鎖定）
2. **第二階段：前端** — C1（login 防迴圈）、C2（exportCsv 失敗處理）、C3（外部瀏覽器兜底）、A2（renderLoc）、A3（照片錯誤 toast）、A5（留言局部刷新）、A6（建單 catalog 失敗防護）、D6/C8、D7/C9
3. **第三階段：後端/DB** — D1（照片查筆數）、D5（索引）、D2（F8 整表重建，高風險）
4. **第四階段：文件/UX** — C4（CLAUDE 標註）、A7（module 封裝備註）、D4（C5 樣式）、D8/C10、D9/C11
5. **維運** — B1（RateLimit CF Dashboard）

> **已排除誤判（補充審查）**：
> - C-3「離線照片佇列」＝審查者誤讀 D7（D7 是編輯權限，無離線功能）
> - #2「`/stats/admin` 權限」＝誤判，`stats.ts` 無 `/admin` 端點（僅 summary/amount-by-category）
> - #3「孤兒照片無清理」＝誤判，SPEC §0.3/§8.6/§10 已明列 v1 不做孤兒照片清理
> - #5「CDN 失效無逃生出口」＝誤判，boot() 尾端已有「完全無 LIFF」fallback（提示＋重新整理）
> - **「找不到路由掛載檔 index.ts」＝誤判**：路由掛載層是 **`src/app.ts`**（非 index.ts），內容含 middleware 掛載順序（csrfGuard → requireAuth → 各 route）。審查者因只看脫敏的 project-overview.md、未見真實 repo 而找不到。

> 註：C1-C3 需 LINE 真機/沙箱驗證（session 過期是否無限重登），超出靜態審查能力，施工後須實測。

---
> 施工完成一項，移入 SPEC 版本歷程 v1.1.15 並於本檔標記。
